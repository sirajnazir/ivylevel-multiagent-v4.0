/**
 * theme.ts
 *
 * Theme utilities and exports for the assessment dashboard.
 */

export { defaultTheme, darkTheme, mergeTheme } from './defaults';
export { colors, typography, spacing, borderRadius, shadows, transitions, breakpoints } from './tokens';
export type { AssessmentTheme } from '../types';
